# Codex Library

This crate provides the abstract logic that all API clients will need if they
will provide codex API access. This includes types that are used in requests
and responses as well as traits needed to allow malleable data usage while
ensuring that the data fits the requirements of each API call
